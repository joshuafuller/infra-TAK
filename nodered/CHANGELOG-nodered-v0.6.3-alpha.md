# Node-RED — v0.6.3-alpha (ArcGIS Configurator + engine)

See **[docs/RELEASE-v0.6.3-alpha.md](../docs/RELEASE-v0.6.3-alpha.md)** for the full operator-facing summary.

**Files touched:** `nodered/build-flows.js`, `nodered/configurator.html`, `nodered/deploy.sh`, `nodered/flows.json`, `nodered/template-functions.json` (regenerated).

**Themes:** multi-layer / multi-geometry Configurator, per-class icons (Step 3c), epoch-ms time filter, template label/remarks, export/import, reconciliation fixes (global `_lastPoll`, tab dedup, stable polygon hash, `_subscribed` reset), Configurator state fixes (`resetForm` on config switch, Step 4 visibility for points).
